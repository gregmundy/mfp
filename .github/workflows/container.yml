name: deploy-container

on:
  push:
    branches:
      - main

jobs:
  do_a_thing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - run: yarn build